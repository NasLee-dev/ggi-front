{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run Docker Compose",
      "type": "shell",
      "command": "bash",
      "args": [
        "-c",
        " docker-compose down && docker-compose up -d"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [],
      "options": {
        "cwd": "${workspaceFolder}" // 현재 작업 디렉토리 설정
      }
    }
  ]
}